(* Space-Sample.
   In Version 0.3 it is possible
   to specify distances of graphical
   objects using the \space-tag.
   All tagparameters can be specified
   in arbitrary units, as is shown in
   the following example. *)

{ [ c \space<2cm> d \space<0> g \space<1in> a ],
  [ \clef<"none"> empty*4/4 \space<0> \clef<"bass"> \space<4cm> \key<3> c#/8 ] }


