% Release 0.8

% noteFormat.gmn

% Use the noteFormat tag to offset complete chords horizontally.
% \noteFormat also colors chords (including stems)

  { [ { f, e2 } ],
    [ \staff<1>  \stemsDown \noteFormat<dx=-0.7hs>( { a, h&0 } ) ] ,

   [ \staff<2> \noteFormat<color="blue">( { c, e, g, a } ) ]  } 

