House keeping when releasing a new version

1) merge dev branch to master branch
2) push back to sf
3) tag the new release with the version number:
   git tag 'vnnn'
4) push the tag to remote hosts
   git push remote --tags

