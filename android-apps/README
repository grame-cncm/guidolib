Make sure to check out all git submodules from the top of the
build tree using:
git submodule update --init --recursive

In the android-cairo directory, call:
  ndk-build

You'll need to do this before building libGUIDOEngine for android.
