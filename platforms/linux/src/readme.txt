This folder includes VGdevice implementations for GNU/Linux. 
Actually, there is an old GTK approach which is probably a bit outdated.
More recently, a Cairo based implementation has been added, that could probably be improved. In particular font management, which is based on the toy API.
